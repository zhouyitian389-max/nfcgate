Trigger release v5.1

Fixes since v5.0:
- FileProvider registered in AndroidManifest (CSV export no longer crashes)
- PhoneNFCHandler: real ReaderMode implementation (NfcAdapter.enableReaderMode)
- NFCEvent.toString() overridden for CardDetected/APDU/MIFARE/Error → readable history rows
- MainActivity "Last Card" label fix (no longer prepends "None")
- INTERNET + NFC permissions added
- Export now writes to cache/exports/ (matches file_provider_paths.xml)
