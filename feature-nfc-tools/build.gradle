plugins {
    id 'com.android.dynamic-feature'
}

android {
    namespace 'de.tu_darmstadt.seemoo.nfcgate.feature.tools'

    compileSdk 35

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 35
    }
}

dependencies {
    implementation project(':app')
    implementation project(':common')
}
