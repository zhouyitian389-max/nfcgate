include ':app', ':app-reader', ':app-hce', ':app-wear', ':common', ':nfcd', ':protobuf'
include ':feature-emv', ':feature-mifare', ':feature-nfc-tools', ':feature-analysis'

buildCache {
    local {
        directory = new File(rootDir, 'build-cache')
    }
}
